# assets

Static assets (logo, icons, images) for the ShopSense AI frontend.

Currently empty - the UI uses emoji and CSS only, no image files. Drop any
image here and reference it from `app.py` with `st.image("assets/<file>")`
if that ever changes.
